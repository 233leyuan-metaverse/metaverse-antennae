interface:
  display_name: "Metaverse Antennae Editor"
  short_description: "Operate Metaverse Antennae safely through SideAPI"
  default_prompt: "Use $metaverse-antennae to inspect the current scene and make a verified editor change."

dependencies:
  tools:
    - type: "mcp"
      value: "antennae_sideapi"
      description: "Local Metaverse Antennae editor SideAPI tools and authoritative knowledge lookups"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
